#include<stdio.h>
#include<conio.h>
int main(){
int  perimeter,length,width,area;
printf("enter length:");
scanf("%d",&length);
printf("enter width:");
scanf("%d,&breath");
perimeter = 2*(length + width);
area = length*width;
return 0;
}
